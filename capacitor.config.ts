import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kevinduncan.kidslearninggame',
  appName: 'Kids Learning Game',
  webDir: 'www'
};

export default config;
